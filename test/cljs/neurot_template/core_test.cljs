(ns neurot-template.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [neurot-template.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
